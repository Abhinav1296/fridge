# Build-time ONLY — the offline embedding bake-off (training + evaluation).
# The deployed app never imports these; it reads the resulting JSON with pure Python.
# Install into a dev environment:  pip install -r requirements-train.txt
gensim>=4.4,<5.0          # scalable word2vec (skip-gram + negative sampling); 4.4 ships cp313 wheels
pandas>=2.0,<3.0          # chunked CSV reading for the multi-GB corpora
scikit-learn>=1.3         # logistic-regression cuisine classifier for the accuracy metric
numpy>=1.26,<3.0          # vectorized cosine / nearest-neighbour scoring (also gensim's dep)
