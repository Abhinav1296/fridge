"""Offline training + evaluation for the ingredient-embedding bake-off.

Nothing here is imported by the deployed app — it runs on a laptop to produce
``data/ingredient_embeddings.json``, which the app then reads with pure Python.
"""
