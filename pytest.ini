[pytest]
# Put the project root on sys.path so tests can `import app`, `import storage`, etc.,
# whether run as `pytest` or `python -m pytest`, locally or in CI.
pythonpath = .
testpaths = tests
addopts = -q
